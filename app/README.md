# ARGUS application boundary

The validated D0A local application currently remains under `services/` and
is orchestrated by the repository-root `docker-compose.yml`.

AWS packaging, bootstrap, and synthetic seed delivery will be added here only
after the BASE substrate and observation attachments validate. No attack
fixture is enabled from this directory. D1 must pass with benign evidence
before D3 stage fixtures are released.
